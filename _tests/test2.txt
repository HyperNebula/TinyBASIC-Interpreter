CLEAR
PRINT "STARTING PROGRAM"
INPUT X, Y
LET Z = -X + Y * ( 10 / 2 )
LET A = +5 - ( Z + X )
IF Z >= 100 THEN PRINT "Z IS LARGE"
IF X <> Y THEN GOTO 500
GOSUB 800
LIST
RUN
END
PRINT "SUBROUTINE START"
RETURN
GOTO 5 + X - (5 + 3) * 4
